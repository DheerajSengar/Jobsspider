[build]
  base = "frontend"
  command = "CI=false npm run build"
  publish = "build"

# Serve React Router paths through the single-page application entry point.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
